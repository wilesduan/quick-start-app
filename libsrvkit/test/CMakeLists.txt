INCLUDE($ENV{CMAKE}/common/prep.cmake)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(libsrvkit.test)

SET(NEED_UTIL TRUE)
SET(NEED_PROTO TRUE)
SET(NEED_PROTOBUF TRUE)
SET(NEED_HIREDIS TRUE)
SET(NEED_JSON TRUE)
SET(NEED_MYSQL TRUE)
SET(NEED_NGHTTP2 TRUE)
SET(NEED_OPENSSL TRUE)
SET(NEED_RDKAFKA TRUE)
SET(NEED_SRVKIT TRUE)
SET(NEED_ZK TRUE)

SET(SOURCE_DIRS ${SOURCE_DIRS} ../src)
SET(EXCLUDE_SOURCE_FILES mysql_test.cc
                         ut_timer.cc
                         ../src/zk.cc)

ENABLE_LANGUAGE(ASM)
SET(INCLUDE_SOURCE_FILES ../src/coctx_swap.S)

INCLUDE($ENV{CMAKE}/common/target.cmake)
